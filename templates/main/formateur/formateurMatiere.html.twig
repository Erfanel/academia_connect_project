{% extends 'formateurBase.html.twig' %}

{% block title %}Formations Formateur
{% endblock %}

{% block main %}
	

	<div class="container rounded  gap-3">
		<div class="d-flex justify-content-between">
			<h2 class="rounded bg-dark text-center text-light p-3 ">{{formation.nom}} / {{ matiere.nom }}</h2>
			<a href="{{ path('formateurFormation', {'formationId': formation.id}) }}" class="btn btn-light h-50">Retour à la page
			{{formation.nom}}</a>
			
		</div>
		

		<div class="rounded bg-light text-dark p-3">
			<div class="d-flex align-items-center gap-3">
				<h2 class="rounded  text-center bg-warning p-3 w-25">{{ matiere.nom }}</h2>
				<a href=" {{ path('formateurModifierProgramme', {'matiereId': matiere.id, 'formationId': formation.id}) }}" class="btn btn-secondary my-3">Modifier le programme</a>
			</div>
			<p class="border rounded p-3">{{ matiere.programme }}</p>

		</div>

		
	</div>

{% endblock %}
