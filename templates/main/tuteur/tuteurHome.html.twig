{% extends 'tuteurBase.html.twig' %}

{% block title %}Accueil Tuteur
{% endblock %}

{% block main %}

	<div class="container rounded w-50 d-flex flex-column justify-content-center text-center gap-3">

		<h1 class="rounded bg-dark text-light mt-5 p-3 ">Bonjour
			{{ user.prenom }}
			{{ user.nom }}</h1>


		<div>
			<h2 class="rounded  text-center bg-warning mb-3 p-3">Vos apprenants</h2>
			<div class="list-group">
				{% for apprenant in apprenants %}
					<a href="{{ path('tuteurApprenant', {'apprenantId': apprenant.id}) }}" class="list-group-item list-group-item-action">
						{{ apprenant.nom }}
						{{ apprenant.prenom }}
					</a>
				{% endfor %}
			</div>
		</div>
	</div>
{% endblock %}
